
_p() {
	if [[ -d "$DOCUMENTATION_HOME/.data/p" ]]; then
		local files_p=("$DOCUMENTATION_HOME/.data/p/$2"*)
		[[ -f ${files_p[0]} ]] && COMPREPLY+=( "$COMPREPLY${files_p[@]##*/}" )
	fi
}

complete -F _p p
