
_dev() {
	# Search in PROJECT_HOME
	if [[ -d "$PROJECT_HOME/src" ]]; then
		local files_dev=("$PROJECT_HOME/src/$2"*)
		[[ -d ${files_dev[0]} ]] && COMPREPLY+=( "$COMPREPLY${files_dev[@]##*/}" )
	fi
}

complete -F _dev dev
