
_dev() {
	# Search in SILVUSPROJECT
	if [[ -d "$SILVUSPROJECT" ]]; then
		local files_dev=("$SILVUSPROJECT/$2"*)
		[[ -d ${files_dev[0]} ]] && COMPREPLY+=( "$COMPREPLY${files_dev[@]##*/}" )
	fi
}

complete -F _dev dev
