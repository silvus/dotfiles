
_dev() {
	# Search in PROJECT_HOME
	if [[ -d "$PROJECT_HOME" ]]; then
		local files_dev=("$PROJECT_HOME/$2"*)
		[[ -d ${files_dev[0]} ]] && COMPREPLY+=( "$COMPREPLY${files_dev[@]##*/}" )
	fi
}

complete -F _dev dev
