#!/usr/bin/env bash

set -e
set -u
set -o pipefail

# Alias of ls

# Sort strategy
# export LC_COLLATE=en_US.UTF-8
# export LC_COLLATE=C.UTF-8

exec ls -l -v --color=tty --almost-all --human-readable --literal --group-directories-first "$@"
