#!/usr/bin/env bash

read_folder="${SILVUSDOC}/.data/readbag"
read_file="${read_folder}/$1"

# Go to folder
cd "$read_folder"

if [[ -f "$read_file" ]]; then
	# Open file
	vim -c "set nonumber norelativenumber" "$read_file"
else
	# Without arguments: Open folder
	ranger
fi

exit 0
