#!/usr/bin/env bash

worklog_file="${DOCUMENTATION_HOME}/.data/worklog/utt.log"

# If --data is set, utt doesn't print help
if [[ -z "$@" ]]; then
	utt
else
	utt --data "$worklog_file" "$@"
fi

exit 0
