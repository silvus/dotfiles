#!/usr/bin/env bash

set -e

TODO_PATH="/data/doc/todo.org"
EMACS_CMD="emacs --init-directory $HOME/.config/emacs -nw"

if [[ -z "$TMUX" ]]; then
	$EMACS_CMD "$TODO_PATH"
else
	tmux_sessionizer -p "$TODO_PATH" -n "todo" -c "$EMACS_CMD"
fi
