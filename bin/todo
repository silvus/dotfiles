#!/usr/bin/env bash

set -e

export TODO_PATH="/data/doc/01_todo.md"

if [[ -z $TMUX ]]; then
	(cd "$NOTE_PATH" && nvim "$TODO_PATH")
else

	if command -v mdorg >/dev/null 2>&1; then
		tmux_sessionizer -p "$TODO_PATH" -c "mdorg"
  else
		tmux_sessionizer -p "$TODO_PATH"
  fi
fi
