#!/usr/bin/env bash 

# Pull/commit and open doc folder with vim
# -----------------------------------------------------------------------------

if [[ -d "$DOCUMENTATION_HOME" ]]; then
    cd "$DOCUMENTATION_HOME"
    git pull origin master
    git add .
    git commit -m "autocommit $(date +'%d/%m/%Y %T')"
    vi
else
    echo "$DOCUMENTATION_HOME is not a folder"
    exit 0
fi

