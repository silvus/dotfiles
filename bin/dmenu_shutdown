#!/usr/bin/env bash

action="$(echo -e "Shutdown\nRestart\nLogout" | dmenu -i)"

case "$action" in
	"shutdown")
		sudo poweroff
		;;
	"restart")
		sudo reboot
		;;
	"logout")
		i3-msg exit
		;;
	*)
		echo "What?"
		;;
esac

exit 0
