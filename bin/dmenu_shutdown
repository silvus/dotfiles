#!/usr/bin/env bash

action="$(echo -e "Shutdown\nRestart\nLogout" | dmenu -i)"

case "$action" in
	"Shutdown")
		sudo poweroff
		;;
	"Restart")
		sudo reboot
		;;
	"Logout")
		i3-msg exit
		;;
esac

exit 0
