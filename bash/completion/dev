
_dev() {
	# Search in GOPATH
	if [[ -d "$GOPATH/src" ]]; then
		local files_go=("$GOPATH/src/$2"*)
		[[ -d ${files_go[0]} ]] && COMPREPLY+=( "$COMPREPLY${files_go[@]##*/}" )
	fi
}

complete -F _dev dev
